# `src/services/` — Business logic (Phase 2+)

Empty in Phase 1 by design. Services orchestrate repositories, own transactions,
and contain business rules. API routes call services; services call
repositories. No service exists yet because Phase 1 adds no business logic and
changes no existing behavior.
