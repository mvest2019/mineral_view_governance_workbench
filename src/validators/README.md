# `src/validators/` — Shared validation utilities (Phase 2+)

Empty in Phase 1 by design. Will hold shared edge-validation (DTO) helpers built
from the enum sets in `src/constants/enums.ts`, mirrored into the database
`$jsonSchema` validators under `src/db/validators/`.
