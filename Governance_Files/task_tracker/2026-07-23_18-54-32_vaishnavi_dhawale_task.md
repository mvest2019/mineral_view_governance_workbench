# Task Tracker

## Employee
Vaishnavi Dhawale

## Created Date
23 July 2026

## Created Time
06:54 PM IST

## Created At
2026-07-23 06:54 PM IST

## Created By
Unknown

## Task Description

Title: MongoDB Cleanup & Standardization — P0

Type: Epic · Priority: High · Status: In Progress (planning done; execution blocked on clean staging)

Description:
Clean up and standardize the ~10-database MongoDB estate (dedup, canonical stores, fix string/mixed types, field-name/typo fixes, add indexes, add schema validation). Approach: staging-first, Expand→Migrate→Contract, reversible cutovers. MongoDB is the serving layer built from PostgreSQL raw data (RRC → PG → Mongo); fixes are in-place in Mongo, PG is reference only.

Done:

- 8 core problems verified on live prod (read-only) + solution per item
- API consumer map + used/not-used collection list (~17 unused)
- Restore script reviewed; Phase-0 scripts drafted (indexes + empty-shell drops)

Current status:

- Staging data restore ~complete (real counts match prod)
- Staging NOT yet a clean clone — extra collections being removed (`ProdMvestPortal` 17→4 leftovers); `Decline_data_to_web` still has extras
- Restore script `--drop` only replaces prod collections, doesn't delete staging-only extras → clean restore needs `dropDatabase` first

---

Generated by Governance Workbench
