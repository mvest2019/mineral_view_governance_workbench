# Task Tracker

## Employee
Vaishnavi Dhawale

## Created Date
2026-07-10

## Created Time
1:21 PM

## Created By
Unknown

## Task Description

1. Database Security Hardening & Infrastructure Cleanup

- Audited the MongoDB and PostgreSQL firewall IP allowlists (~200 IPs).
- Identified that approximately **175 IPs** belonged to developers' home/mobile networks, creating unnecessary security exposure due to dynamic IP addresses.
- Removed obsolete office network entries and verified the remaining IPs through geolocation.
- Reduced the allowlist to approximately **33 production server/infrastructure IPs**, with developers transitioning to VPN-based access.
- Rotated the Windows production server (RDP) login credentials to improve infrastructure security.
- Investigated and documented two critical data-quality issues:
  - Valuation fields were stored as text, causing monetary totals to be calculated as zero.
  - Identified the root cause of **"MVEstimate = $0"** as depleted leases being stored with `NaN` values.
- Reported both data-quality issues to **Nikhil** for further remediation.
- Improved the overall database engineering assessment from an initial **3.5/10** to approximately **6.3/10 for MongoDB** and **5.3/10 overall** through security hardening and infrastructure cleanup.

2. Frontend ↔ Backend API Audit & Cleanup

- Performed a comprehensive audit of API usage between the **Mview-Presentation-Next** frontend and all backend services.
- Generated an endpoint inventory containing **245 frontend-consumed APIs** across the Presentation, Portal, Map, and Community services.
- Identified **97 backend APIs** with no frontend consumers:
  - Presentation API: **66**
  - Portal API: **9**
  - Map API: **2**
  - Community API: **20**
- In the Presentation API repository:
  - Marked all **66 unused APIs** with audit comments.
  - Renamed **57 confirmed unused routes** using the `unused_` prefix (repository convention) to disable legacy endpoints.
  - Retained **9 potentially risky routes** (marketing/sitemap and member verification endpoints) as comment-only to avoid impacting external or non-frontend consumers.
- Validated all modified files using `node --check`; changes remain uncommitted pending production access-log verification.

3. Texas Highlights API Enhancement

- Updated the **GET `/api/market/texas-highlights`** endpoint to source permit statistics from PostgreSQL instead of MongoDB.
- Migrated **statewidePermitsLast30d** to query the **w1permits** table in the **MviewDownload** database using `PG_CONNECTIONSTRING2`.
- Implemented a rolling **30-day reporting window** ending **two days before the current date** to account for data availability.
- Applied the required business filters:
  - `status = 'Approved'`
  - `filing_purpose = 'New Drill'`
- Implemented an optimized PostgreSQL query using `issued_date BETWEEN` for accurate permit counting.
- Preserved the existing API response contract by keeping the response key **statewidePermitsLast30d** unchanged.
- Documented a follow-up recommendation to migrate **topCountyByPermits** and **hottestPlay** from MongoDB to PostgreSQL for complete data consistency across the endpoint.

---

Generated by Governance Workbench
